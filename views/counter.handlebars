<header class="greetedList"><h1>Greeted List</h1></header>
<div class="greetlist">
    <div  class="contTable">
<table>
    <tr>
        <th class="tableHead"><h5>Name</h5></th>
        <th></th>

    </tr>

    {{#each greetedNames}}
        <tr>
        <td><a href="/counted/{{name}}" class="names">{{name}}</a></td>
        <td><form action="/counter/delete/{{name}}" method="POST"> <button class="deleteButton">Delete</button></form></td>
            </tr>
    {{/each}}


</table>
</div>
<div class="status">
 <a  href="/"><button class="status back">BACK</button></a>
 </div>
</div>
 </div>